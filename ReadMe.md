## 目录说明

```
  + src
      index.js // 入口
      + component // 组件
      + styles // 样式
      + store // store state
        + Actions // action
        + Reducers // Reducers
      + static // 静态资源包括 mock 等
```